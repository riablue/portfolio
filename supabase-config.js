// Fill these in after creating your Supabase project (see SETUP.md).
// Leave both empty to keep working in local mode (data stays in this browser).
window.RIA_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  // Google accounts allowed into backstage. Must also be listed in the `admins` table (SETUP.md step 3).
  ADMIN_EMAILS: ['riablue.collab@gmail.com']
};
